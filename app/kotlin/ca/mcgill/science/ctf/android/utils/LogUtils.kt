package ca.mcgill.science.ctf.android.utils

import android.util.Log

private const val TAG = "CTFA"

fun logd(text: String) = Log.d(TAG, text)
fun logi(text: String) = Log.i(TAG, text)
fun loge(text: String) = Log.e(TAG, text)